import React from "react";
import { Paper } from "@material-ui/core/";

export default props => (
  <Paper square item sm= "6"  style={{textAlign: "center" , padding: 5, marginTop: 1, marginBottom: 3 }}>
    hi there
  </Paper>
);
